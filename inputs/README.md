# Inputs

Input firmware blobs and related assets belong here.

## Current Layout

- `firmware/`: raw firmware binaries and related input files used for extraction

Generated outputs should not be written back into `inputs/`; they belong in `extraction_out/`.
