{
  "function": "rf_fault_dump_n_8",
  "image": "fmacfw_8800d80_u02_bin",
  "addr": "0x114740",
  "result": {
    "divergences": [
      {
        "type": "missing",
        "original": "0x00114740      f432           adds r2, 0xf4",
        "compiled": "0x0010d6f2      0043           orrs r0, r0",
        "fix_hint": "Original is a tiny 2-instruction stub (adds r2, 0xf4; movs r7, r2); compiled region contains unrelated code (store sequence), not the rf_fault_dump_n_8 function body at all."
      },
      {
        "type": "missing",
        "original": "0x00114742      1700           movs r7, r2",
        "compiled": "0x0010d6f4      c867           str r0, [r1, 0x7c]",
        "fix_hint": "Second instruction of the original (movs r7, r2) is entirely absent; compiled code writes r0 to memory and continues with a completely different control flow."
      },
      {
        "type": "wrong_op",
        "original": "adds r2, 0xf4 / movs r7, r2  (purely register arithmetic, no memory access)",
        "compiled": "orrs r0, r0 / str r0, [r1, 0x7c] / str r3, [r2] / ldr r3, [...] / bx lr",
        "fix_hint": "Reconstructed C source compiled to the wrong function body \u2014 it performs pointer stores and a return, whereas the original is an in-line constant add-and-move helper (likely part of a switch/case constant-table encoding for 'n_8' = 0xf4)."
      }
    ],
    "match_pct": 0,
    "concerns": [
      "Fundamental structural mismatch: the ORIGINAL rf_fault_dump_n_8 is a 4-byte, 2-instruction Thumb stub (adds r2,#0xf4; movs r7,r2) that looks like an index-offset/case-value thunk, not a real function.",
      "The COMPILED region at 0x10d6f2 belongs to an entirely different routine (a register-store/return sequence with a pop {r4-r7}; bx lr epilogue visible nearby at 0x10d6fc/0x10d700).",
      "The user's reported 'COMPILED DISASM' showing all 0xFFFFFFFF at 0x10d6f2 is inconsistent with the actual binary bytes (0x00,0x67,0xc8,0x67,0x13,0x60,0x1c,0x4b) \u2014 raw bytes are valid Thumb, so the 0xFFFFFFFF listing likely comes from a wrong image/section or a broken disassembly pass, not from the real compiled output.",
      "No callees were found for rf_fault_dump_n_8 and no xrefs point at 0x114740, suggesting it is reached only via an indirect jump table or computed branch \u2014 the mechanical reconstructor likely never matched the thunk pattern and emitted a generic stub instead.",
      "Immediate 0xf4 is suspicious: 0xf4 = 244, but for a case labeled 'n_8' (n=8) one would expect 8*something (e.g. 8*0x1f=0xf8 or 8*0x1e=0xf0). The literal 0xf4 may itself be a decoded switch-table slot rather than a meaningful constant \u2014 worth double-checking the decompiler's constant recovery."
    ]
  }
}