{
  "cluster_id": "0|40|40200014",
  "size": 96,
  "pattern": {
    "pattern_name": "rf_bus_regfile_drain",
    "description": "Reads (and occasionally writes) a sequence of MMIO registers from an RF bus register file at fixed offsets (0x0, 0x40, 0x60, 0x80, 0xa0, ...), discarding results",
    "category": "mmio_read",
    "template_c": "void rf_bus_drain(volatile uint32_t *base) {\n    (void)base[0x00/4];\n    (void)base[0x40/4];\n    (void)base[0x60/4];\n    (void)base[0x80/4];\n    (void)base[0xa0/4];\n    (void)base[0xc0/4];\n}"
  }
}