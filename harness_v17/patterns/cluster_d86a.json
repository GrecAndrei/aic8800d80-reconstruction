{
  "cluster_id": "182750|2007fed0|2007fed4",
  "size": 130,
  "pattern": {
    "pattern_name": "rf_bus_command_issue",
    "description": "Issues an RF bus command by writing a 4-dword header (cmd id + 3 zeros) to 0x2007fef0..0x2007fefc, then writing payload/trigger to 0x2007fed0 and follow-on dwords, optionally preceded by a read-modify-write on a control register like 0x40240210.",
    "category": "mmio_write",
    "template_c": "static inline void rf_bus_command_issue(uint32_t cmd_id, uint32_t *payload, unsigned n) {\n    volatile uint32_t *hdr = (volatile uint32_t *)0x2007fef0U;\n    hdr[0] = cmd_id; hdr[1] = 0; hdr[2] = 0; hdr[3] = 0;\n    volatile uint32_t *trg = (volatile uint32_t *)0x2007fed0U;\n    for (unsigned i = 0; i < n; i++) trg[i] = payload[i];\n}"
  }
}